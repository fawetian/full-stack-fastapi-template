# =============================================================================
# FastAPI Full Stack Template - 环境变量配置
# =============================================================================
# 此文件包含所有环境变量的说明和默认值
# 复制此文件为 .env 并根据需要修改
#
# Docker 模式和非 Docker 模式都使用此配置文件
# 主要区别在于 POSTGRES_SERVER 的值：
#   - Docker 模式: POSTGRES_SERVER=db (容器名)
#   - 非 Docker 模式: POSTGRES_SERVER=localhost
# =============================================================================

# -----------------------------------------------------------------------------
# 环境设置
# -----------------------------------------------------------------------------
# 环境类型: local (开发), staging (测试), production (生产)
ENVIRONMENT=local

# 项目名称 - 显示在 API 文档和邮件中
PROJECT_NAME="FastAPI Project"

# 域名 - 用于生成链接和 CORS 配置
# Docker 模式本地开发: localhost 或 localhost.tiangolo.com
# 非 Docker 模式本地开发: localhost
# 生产环境: your-domain.com
DOMAIN=localhost

# 前端地址 - 用于 CORS 和重定向
FRONTEND_HOST=http://localhost:5173

# -----------------------------------------------------------------------------
# 安全配置 (生产环境必须修改!)
# -----------------------------------------------------------------------------
# JWT 密钥 - 用于签署 token
# 生成方法: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=changethis

# 第一个超级用户
FIRST_SUPERUSER=admin@example.com
FIRST_SUPERUSER_PASSWORD=changethis

# -----------------------------------------------------------------------------
# 数据库配置
# -----------------------------------------------------------------------------
# Docker 模式: POSTGRES_SERVER=db
# 非 Docker 模式: POSTGRES_SERVER=localhost
POSTGRES_SERVER=db
POSTGRES_PORT=5432
POSTGRES_USER=app
POSTGRES_PASSWORD=changethis
POSTGRES_DB=app

# -----------------------------------------------------------------------------
# 邮件配置 (可选)
# -----------------------------------------------------------------------------
# SMTP 服务器设置
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_TLS=true
SMTP_SSL=false

# 发件人信息
EMAILS_FROM_EMAIL=info@example.com
EMAILS_FROM_NAME="FastAPI Project"

# -----------------------------------------------------------------------------
# Docker Compose 专用配置
# -----------------------------------------------------------------------------
# Docker 镜像名称
DOCKER_IMAGE_BACKEND=backend
DOCKER_IMAGE_FRONTEND=frontend

# 堆栈名称 - 用于 Docker Compose 和 Traefik 标签
# 不同环境应使用不同名称，如: myproject-staging, myproject-production
STACK_NAME=fastapi-project

# -----------------------------------------------------------------------------
# 可选配置
# -----------------------------------------------------------------------------
# CORS 允许的源 (逗号分隔)
BACKEND_CORS_ORIGINS=

# Sentry DSN (错误追踪，可选)
SENTRY_DSN=
